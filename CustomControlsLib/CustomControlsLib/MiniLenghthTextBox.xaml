<UserControl x:Class="CustomControlsLib.MiniLenghthTextBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CustomControlsLib"
            >
    <StackPanel>
        <Border x:Name="border" CornerRadius="1" Padding="1" BorderBrush="#D5D8DC" BorderThickness="1">
            <TextBox x:Name="textBox"
              BorderThickness="0"
          Text="{Binding MiniLengthText, RelativeSource={RelativeSource AncestorType=UserControl}, UpdateSourceTrigger=PropertyChanged}"
          VerticalContentAlignment="Center"/>
        </Border>
    </StackPanel>
</UserControl>
